# CNI-BBS 中间件环境变量配置
# 将此文件复制为 .env 并根据实际情况修改配置

# ==================== Elasticsearch 配置 ====================
# Elasticsearch HTTP 端口
ES_PORT=9200

# Elasticsearch 节点名称
NODE_NAME=titan-bbs-node

# Elasticsearch 集群名称
CLUSTER_NAME=titan-bbs-cluster

# Elasticsearch 版本
STACK_VERSION=8.16.1

# Elasticsearch 密码（elastic 用户）
ELASTIC_PASSWORD=titan_bbs_2026

# ==================== MySQL 配置 ====================
# MySQL 端口
MYSQL_PORT=13306

# MySQL Root 密码（管理员）
MYSQL_ROOT_PASSWORD=titan_bbs_root_2026

# MySQL 数据库名称
MYSQL_DATABASE=titan_bbs

# MySQL 应用用户名
MYSQL_USER=titan_bbs

# MySQL 应用用户密码
MYSQL_PASSWORD=titan_bbs_2026

# ==================== Redis 配置 ====================
# Redis 端口
REDIS_PORT=6379

# Redis 密码（在 redis.conf 中配置，这里仅作说明）
REDIS_PASSWORD=titan_bbs_2026

# ==================== RabbitMQ 配置 ====================
# RabbitMQ AMQP 端口
RABBITMQ_PORT=5672

# RabbitMQ 管理界面端口
RABBITMQ_MANAGEMENT_PORT=15672

# RabbitMQ 用户名
RABBITMQ_USER=titan_bbs

# RabbitMQ 密码
RABBITMQ_PASSWORD=titan_bbs_2026

# RabbitMQ 虚拟主机
RABBITMQ_VHOST=/titan-bbs

# ==================== MinIO 配置 ====================
# MinIO API 端口（S3 兼容接口）
MINIO_API_PORT=9000

# MinIO 控制台端口
MINIO_CONSOLE_PORT=9001

# MinIO Root 用户（Access Key）
MINIO_ROOT_USER=titan_bbs

# MinIO Root 密码（Secret Key）
MINIO_ROOT_PASSWORD=titan_bbs_2026

# ==================== 生产环境建议 ====================
# 生产环境请修改以下配置：
# 1. 所有密码改为强密码（16 位以上，包含大小写字母、数字、特殊字符）
# 2. 限制端口映射范围，不要暴露到公网
# 3. 使用 Docker Secrets 管理敏感信息
# 4. 启用 SSL/TLS 加密传输
# 5. 配置防火墙规则
