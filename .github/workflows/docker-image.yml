name: CI/CD for TitanCore

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout to Main
      uses: actions/checkout@v4
      with:
        ref: main

    - name: Set up JDK 17 with Maven cache
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'maven'
        cache-dependency-path: '**/pom.xml'

    - name: Generate application-docker.properties
      run: |
        mkdir -p bbs-core/src/main/resources
        echo "${{ secrets.DOCKER_PROPERTIES }}" > bbs-core/src/main/resources/application-docker.properties

    - name: Build with Maven
      run: mvn package -B -Pdocker -DskipTests

    - name: Set up QEMU (for multiarch)
      uses: docker/setup-qemu-action@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      with:
        install: true

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Get timestamp
      id: timestamp
      run: |
        echo "IMAGE_TAG=$(date +%Y%m%d%H%M%S)" >> "$GITHUB_OUTPUT"
        echo "SHORT_SHA=${GITHUB_SHA::6}" >> "$GITHUB_OUTPUT"

    - name: Build and push multi-arch Docker image to Docker Hub
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        platforms: linux/amd64,linux/arm64
        push: true
        tags: |
          patricleee/cni-bbs-core:latest
          patricleee/cni-bbs-core:${{ steps.timestamp.outputs.IMAGE_TAG }}${{ steps.timestamp.outputs.SHORT_SHA }}

#    - name: Deploy to server via SSH
#      uses: appleboy/ssh-action@v1.0.3
#      with:
#        host: ${{ secrets.SSH_HOST }}
#        username: ${{ secrets.SSH_USER }}
#        key: ${{ secrets.SSH_KEY }}
#        port: 221
#        script: |
#          echo ${{ secrets.DOCKERHUB_TOKEN }} | sudo docker login --username=${{ secrets.DOCKERHUB_USERNAME }} --password-stdin
#          sudo docker pull patricleee/cni-bbs-core:${{ steps.timestamp.outputs.IMAGE_TAG }}${{ steps.timestamp.outputs.SHORT_SHA }}
#          sudo docker stop titan-core || true
#          sudo docker rm titan-core || true
#          sudo docker run -d --name titan-core -p 8999:8080 --network 1panel-network patricleee/cni-bbs-core:${{ steps.timestamp.outputs.IMAGE_TAG }}${{ steps.timestamp.outputs.SHORT_SHA }}
#          sudo docker network connect es-net titan-core
